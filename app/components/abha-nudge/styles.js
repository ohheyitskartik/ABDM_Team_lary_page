import { StyleSheet } from 'react-native';
import { scale } from '../../../utils';

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        flexDirection: 'row',
        borderWidth: 2,
        borderRadius: 15,
        borderColor: 'rgb(254, 245, 189)',
        backgroundColor: 'rgba(254, 245, 189, 0.4)',
    },
});

export default styles;
